services:

  sticker-bot:

    build:
      context: .

    container_name: whatsapp-sticker-bot

    restart: always

    volumes:
      - ./sessions:/app/storage/sessions
      - ./temp:/app/temp
      - ./groups.json:/app/groups.json:ro
      - ./storage:/app/storage
